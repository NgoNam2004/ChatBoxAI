// Auth feature placeholder.
// Header currently links to "#login" as a static anchor; once real
// authentication is built, add components/ (LoginForm, RegisterForm),
// hooks/ (useAuth), and export the public API from here — mirroring
// the chat and home features.
export {}
