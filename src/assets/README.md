# assets/

Local static assets (logo files, icons, local images) go here. The site
currently sources its imagery from Unsplash URLs directly in
`src/features/home/components/Hero.jsx`, so this folder is empty for now.
